@model IEnumerable<BDCrudTestMVC.Models.CoCategorium>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Crear nuevo registro</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CNombCateg)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CEsActiva)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CNombCateg)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CEsActiva)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NIdCategori">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NIdCategori">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NIdCategori">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
